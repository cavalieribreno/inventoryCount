export interface Product {
    productName: string;
    code: string;
    year: number;
    totalQuantity: number;
}
export interface ProductDetails{
    id: number,
    code: string,
    year: number,
    quantity: number,
    dateHour: string
}